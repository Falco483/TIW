<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
    ===========================================================================
    POM PADRE — tiw-parent
    ===========================================================================
    PERCHÉ ESISTE:

    Un progetto Maven Multi-Module ha un unico POM padre che:
    1. Dichiara la lista dei moduli figli in <modules>
    2. Centralizza le versioni delle dipendenze in <dependencyManagement>
    3. Centralizza la configurazione dei plugin in <pluginManagement>

    Il vantaggio è DRY: se domani aggiorniamo Jackson da 2.17 a 2.18,
    lo cambiamo QUI e tutti i moduli ereditano la nuova versione.
    Senza il POM padre, dovresti aggiornare 3 pom.xml separatamente,
    con il rischio di disallineamento (modulo A usa Jackson 2.17,
    modulo B usa 2.18 → classloader hell).

    PACKAGING = pom:
    Questo POM non produce nessun artefatto (no jar, no war).
    È solo un coordinatore. Maven lo usa per orchestrare il build
    dei figli nell'ordine corretto (tiw-core prima, poi i WAR).
    ===========================================================================
    -->

    <groupId>it.polimi.tiw</groupId>
    <artifactId>tiw-parent</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>TIW Configuratore — Parent</name>

    <!-- ===================================================================
         ORDINE DEI MODULI:
         Maven compila nell'ordine in cui li dichiariamo QUI,
         ma risolve automaticamente le dipendenze tra moduli.
         tiw-core DEVE stare prima perché tiw-ssr e tiw-spa dipendono da lui.
         =================================================================== -->
    <modules>
        <module>tiw-core</module>
        <module>tiw-ssr</module>
        <module>tiw-spa</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>

        <!-- Versioni centralizzate: un solo punto di verità -->
        <jakarta.servlet.version>6.0.0</jakarta.servlet.version>
        <mysql.connector.version>8.3.0</mysql.connector.version>
        <jackson.version>2.17.0</jackson.version>
        <thymeleaf.version>3.1.2.RELEASE</thymeleaf.version>
        <jbcrypt.version>0.4</jbcrypt.version>
    </properties>

    <!--
    ===========================================================================
    dependencyManagement vs dependencies:

    - dependencyManagement: DICHIARA le versioni ma NON le aggiunge ai moduli.
      I moduli figli devono comunque dichiarare la dipendenza nel loro pom.xml,
      ma possono omettere la <version> perché la ereditano dal padre.

    - dependencies: AGGIUNGE la dipendenza a TUTTI i moduli figli.
      Se mettessimo Jackson qui, anche tiw-ssr (che non lo usa) lo includerebbe
      nel suo WAR → spreco di spazio, potenziali conflitti.

    Regola: usare dependencyManagement nel padre, dependencies nei figli.
    ===========================================================================
    -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>jakarta.servlet</groupId>
                <artifactId>jakarta.servlet-api</artifactId>
                <version>${jakarta.servlet.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>com.mysql</groupId>
                <artifactId>mysql-connector-j</artifactId>
                <version>${mysql.connector.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.datatype</groupId>
                <artifactId>jackson-datatype-jsr310</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>org.thymeleaf</groupId>
                <artifactId>thymeleaf</artifactId>
                <version>${thymeleaf.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mindrot</groupId>
                <artifactId>jbcrypt</artifactId>
                <version>${jbcrypt.version}</version>
            </dependency>
            <!-- Riferimento al modulo core per i WAR -->
            <dependency>
                <groupId>it.polimi.tiw</groupId>
                <artifactId>tiw-core</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>17</source>
                        <target>17</target>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <version>3.4.0</version>
                    <configuration>
                        <failOnMissingWebXml>false</failOnMissingWebXml>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
